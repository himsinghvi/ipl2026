/* Global Chart.js configuration for dark theme */
if (typeof Chart !== 'undefined') {
    Chart.defaults.color = '#adb5bd';
    Chart.defaults.borderColor = '#33373a';
    Chart.defaults.font.family = "'Inter', system-ui, sans-serif";
}
